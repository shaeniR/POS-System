# Copy this file to .env to change where the frontend finds the backend.

# Development: the Vite dev server forwards /api to this URL (default http://localhost:8081).
VITE_BACKEND_URL=http://localhost:8081

# Deployment: the backend's public URL used by the built app. Leave empty in development.
VITE_API_BASE_URL=
