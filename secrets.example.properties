# Copy this file to secrets.properties (same folder) and fill in your own values.
# secrets.properties is ignored by git, so your password never leaves your computer.
#
# On a hosting provider, set these as environment variables instead.

# Required
DB_PASSWORD=your-mysql-password

# Optional (defaults shown)
# DB_URL=jdbc:mysql://localhost:3306/pos_inventory_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
# DB_USERNAME=root
# CORS_ALLOWED_ORIGINS=http://localhost:5173
# SHOW_SQL=false
